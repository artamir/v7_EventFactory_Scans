Перем Таймаут Экспорт, ВремяЗавершения;

//Функция ТотЖе(Значение) Возврат Значение; КонецФункции
Функция Сам(Конт) Возврат Конт; КонецФункции
Функция База(ИмяКласса) Возврат Сам(Контекст).ПолучитьБазовыйКласс(ИмяКласса); КонецФункции

Процедура Конструктор()
	Таймаут=300;
КонецПроцедуры
//--------------------------------------------

//Процедура Запустить(Результат) Экспорт 
//	Сам().ПолучитьБазовыйКласс().Запустить(Результат);
//	ВремяЗавершения=_GetPerformanceCounter()+Таймаут;
//КонецПроцедуры
//--------------------------------------------

//Функция Завершен() Экспорт
//	Если ТипЗначенияСтр(Сам().КонтекстФормы)<>"ГрупповойКонтекст" Тогда Возврат 1; КонецЕсли;
//	Если _GetPerformanceCounter()>ВремяЗавершения Тогда
//		Модуль=СоздатьОбъект("ВыполняемыйМодуль");
//		Модуль.ВыброситьИскл("Истекло время закрытия формы теста", "Тест: "+Сам().ИмяМетода);
//	КонецЕсли;
//	Сам().КонтекстФормы.Форма.Закрыть(0);
//	Возврат 0;
//КонецФункции
////--------------------------------------------

Процедура Сброс() Экспорт
	Сам(Контекст).ПолучитьБазовыйКласс("ДелегатФормы").ЗакрытьФорму();
	Сам(Контекст).ПолучитьБазовыйКласс("ТестовыйСлучай").Сброс(); // обязательно !!
КонецПроцедуры
