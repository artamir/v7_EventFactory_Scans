
Функция Сам(Конт)
	Возврат Конт;
КонецФункции

Функция вСтроку() Экспорт
	Результат = "";
	Для С=1 По Сам(Контекст).КоличествоСтрок() Цикл
		Результат = Результат + Сам(Контекст).ПолучитьСтроку(С) + РазделительСтрок;
	КонецЦикла;
	
	Возврат Лев(Результат, СтрДлина(Результат) - 2); // отрезали последний РазделительСтрок.
КонецФункции

Процедура ДобавитьШапкуУпавшегоТеста(Тест) Экспорт
	Сам(Контекст).ДобавитьСтроку("=====================");
	Сам(Контекст).ДобавитьСтроку(""+Тест.ПолучитьКаталог()+" Тест №"+Тест.ПолучитьНомер());
	Сам(Контекст).ДобавитьСтроку("--------------------------");
	Сам(Контекст).ДобавитьСтроку("");
	Сам(Контекст).ДобавитьСтроку("Тест не пройден. "+Тест.ОшибкаПоКоду(Тест._КодРазличий));
	Сам(Контекст).ДобавитьСтроку("");
КонецПроцедуры