
Функция Сам(Конт)
	Возврат Конт;
КонецФункции

Процедура Тест_ТекстИзДвухСтрок() Экспорт
	Текст = СоздатьОбъект("TextTestResult");
	Текст.ДобавитьСтроку("Первая строка");
	Текст.ДобавитьСтроку("Вторая строка");
	
	Сам(Контекст).ПроверитьРавенство(Текст.вСтроку(), "Первая строка"+РазделительСтрок+"Вторая строка");
КонецПроцедуры

Процедура Тест_ПустойТекст() Экспорт
	Текст = СоздатьОбъект("TextTestResult");
	
	Сам(Контекст).ПроверитьРавенство(Текст.вСтроку(), "");
КонецПроцедуры
